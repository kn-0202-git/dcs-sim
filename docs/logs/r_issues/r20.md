r-issue: r20 / Step5事前コードレビュー: SVGインポートのセキュリティ・運用リスク / 改善 / 高 / 🟡 open
detail: feature/step5-svg-import 着手前レビューで、複数観点の課題を確認。(1) セキュリティ観点: Step5方針が「アップロードSVGをそのまま表示 + DOM操作」であり、未サニタイズのSVGを扱う設計のため、script/event属性/外部参照混入によるXSS・情報漏えいリスクがある（docs/logs/handover/HANDOFF_2026-02-09.md, docs/guides/PLANT_CUSTOMIZATION.md）。(2) 教育/運用観点: CSVパーサーが `split(',')` のみで、説明文にカンマが入ると列ずれして訓練手順・ルール誤読を起こしうる（src/education/csvParser.ts）。(3) シニアエンジニア観点: PIDSimulator で `phasesData.sort(...)` を直接実行して state 配列を破壊的変更しており、将来のデータソース切替時に順序不整合や追跡困難な不具合の原因になる（src/components/PIDSimulator.tsx）。
action: Step5-1 の完了条件に「SVGサニタイズ/禁止属性除去/ファイルサイズ上限/解析失敗時の安全フォールバック」を追加。CSVはクォート対応パーサーへ置換（または実装強化）し、カンマ含有ケースのテストを追加。PIDSimulatorは非破壊ソートへ変更し、order不正値のバリデーションを追加。各対応をStep5-1〜Step5-4の受け入れ条件に反映して実装する。
edited documents: docs/logs/r_issues/r20.md, docs/logs/r_issues/meta.yaml, docs/process/PLAN.md
