r-issue: r18 / brew shellenv の毎回手動実行を不要にする / 改善 / 中 / ✅ resolved
detail: Claude Code の Bash ツールで npm 等を実行する際、毎回 `eval "$(/opt/homebrew/bin/brew shellenv)"` を前置する必要があった。原因は `~/.zprofile` にのみ設定があり、非ログインシェルで実行される Claude Code Bash ツールでは読み込まれなかったため。
action: `~/.zshrc` に `eval "$(/opt/homebrew/bin/brew shellenv)"` を追加。MEMORY.md の運用ルールも更新。
edited documents: ~/.zshrc, MEMORY.md, docs/logs/r_issues/r18.md, docs/logs/r_issues/meta.yaml, docs/process/PLAN.md
