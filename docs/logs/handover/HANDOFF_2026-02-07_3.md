# 引き継ぎメモ（2026-02-07 #3）

## 1. 目的
- 次のLLMが直前までの作業と理由を短時間で把握できるようにする。

## 2. 何をやったか
- シニアエンジニアとしてコードレビューを実施し、P0〜P2の指摘事項をまとめた
- Step 3（全5 issue）を計画・実装完了

### Step3-1: データ層の疎結合化
- `sampleData.ts`: allValveIds/allTankIds/nodeMap/initialTankFilled/initialValves を nodes/pipes から自動導出に変更
- `sampleData.ts`: tankIdMap（CSV条件短縮名→ノードIDマッピング）を追加
- `conditionEvaluator.ts`: tankIdMap パラメータ追加（デフォルト従来互換）
- `ruleCheckEngine.ts`: tankIdMap パラメータ追加
- `PIDCanvas.tsx`: キャンバスサイズをノード座標から自動算出
- `PIDSimulator.tsx`: tankIdMap 引き渡し、devモードでトポロジーバリデーション実行
- `topologyValidator.ts`: 新規作成（ノード/パイプ/バルブID整合性チェック）
- テスト9件追加（topologyValidator）

### Step3-3: 安全性強化
- `conditionEvaluator.ts`: 未知条件を `true` → `false` に変更（fail-safe）
- `CSVEditorPanel.tsx`: 全行バリデーション（先頭行のみ→全行チェック）
- `PIDSimulator.tsx`: checkRules 呼び出しに try-catch 追加
- 既存テスト1件更新（未知条件の期待値変更）

### Step3-2: Docker 環境構築
- `Dockerfile`: マルチステージビルド（dev/build/prod）
- `docker-compose.yml`: dev（Vite HMR, port 5173）+ prod（serve, port 3000）
- `.dockerignore`: node_modules, .git, docs/logs 等除外
- `.nvmrc`: Node 22 固定

### Step3-4: プラント差し替えガイドライン
- `docs/guides/PLANT_CUSTOMIZATION.md`: 新規作成
  - トポロジーデータ作成手順、CSV条件式文法、座標設計ガイド、バリデーション使い方、チェックリスト、サンプルコード

### Step3-5: 整理・クリーンアップ
- `training-simulator-csv.jsx`: deprecated コメント追加
- `.editorconfig`: 新規作成
- `docs/process/PLAN.md`: Step 3 全issue resolved

## 3. 実行コマンド
- セットアップ: `eval "$(/opt/homebrew/bin/brew shellenv)" && npm install`
- 開発: `npm run dev`
- テスト: `npm test`（全95件パス）
- ビルド: `npm run build`
- Docker dev: `docker compose up dev` → http://localhost:5173
- Docker prod: `docker compose up prod` → http://localhost:3000

## 4. 成果物/主要ファイル
### 今回作成・変更したファイル
- `src/data/sampleData.ts` — 自動導出化 + tankIdMap追加
- `src/data/topologyValidator.ts` — 新規
- `src/data/__tests__/topologyValidator.test.ts` — 新規（9件）
- `src/education/conditionEvaluator.ts` — tankIdMap対応 + fail-safe
- `src/education/ruleCheckEngine.ts` — tankIdMap対応
- `src/education/__tests__/conditionEvaluator.test.ts` — 期待値更新
- `src/components/PIDCanvas.tsx` — キャンバスサイズ自動算出
- `src/components/PIDSimulator.tsx` — tankIdMap/validator/try-catch
- `src/components/CSVEditorPanel.tsx` — 全行バリデーション
- `Dockerfile` — 新規
- `docker-compose.yml` — 新規
- `.dockerignore` — 新規
- `.nvmrc` — 新規
- `.editorconfig` — 新規
- `docs/guides/PLANT_CUSTOMIZATION.md` — 新規
- `docs/logs/issues/Step3-1〜3-5.md` — 新規
- `docs/process/PLAN.md` — Step 3 追記

## 5. 次のアクション
- Step 3 完了。未コミット。コミットが必要
- Docker 実動作確認（`docker compose up dev` / `docker compose up prod`）
- ブラウザ目視確認（`npm run dev` でバルブ操作・訓練モードの動作検証）
- 候補: CI/CD（GitHub Actions）、追加トポロジー、UI改善

## 6. LLM / 日付
- LLM: Claude Opus 4.6
- 日付: 2026-02-07

## 7. 注意点/リスク
- Docker イメージは node:22-slim を使用（ローカル開発は Node 25 だが Docker LTS で安定運用）
- tankIdMap はタンクの nodes 配列出現順で番号付け（T1=最初のtank、T2=2番目のtank）
- conditionEvaluator の未知条件は false を返すようになった（旧: true）。CSV設定で不正な条件式を書くとステップ完了判定に影響
- `training-simulator-csv.jsx` は deprecated だが削除はしていない（参照用に残存）

## 8. テスト状況
- テスト: 95件全パス（旧86件 + topologyValidator 9件）
- ビルド: 成功（212KB / 67KB gzip）
- テスト内訳:
  - logic: 20件（computeReachableNodes 13, isPipeActive 7）
  - education: 52件（csvParser 10, conditionEvaluator 31, ruleCheckEngine 11）
  - data: 9件（topologyValidator 9）
  - component: 14件（PIDSimulator 14）
